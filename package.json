{
  "name": "my-homepage",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "datamaps": "^0.5.9",
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  },
  "scripts": {
    "clean": "rm -rf build/*",
    "copy-assets": "mkdir -p build && cp -r public/* build",
    "bundle": "npx webpack",
    "build": "npm run copy-assets && npm run bundle && cp index.html build && cp error.html build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "webpack": "^4.16.0",
    "babel-cli": "^6.26.0",
    "babel-loader": "^7.1.5",
    "babel-preset-react-app": "^3.1.2",
    "webpack-cli": "^3.0.8",
    "css-loader": "^1.0.0",
    "style-loader": "^0.21.0"
  }
}
